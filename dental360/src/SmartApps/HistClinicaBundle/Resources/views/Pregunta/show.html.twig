{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Pregunta</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Enunciado</th>
                <td>{{ entity.enunciado }}</td>
            </tr>
            <tr>
                <th>Obligatoria</th>
                <td>{{ entity.obligatoria }}</td>
            </tr>
            <tr>
                <th>Orden</th>
                <td>{{ entity.orden }}</td>
            </tr>
            <tr>
                <th>Filacompleta</th>
                <td>{{ entity.filaCompleta }}</td>
            </tr>
            <tr>
                <th>Nocolumna</th>
                <td>{{ entity.noColumna }}</td>
            </tr>
            <tr>
                <th>Estaactiva</th>
                <td>{{ entity.estaActiva }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('pregunta') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('pregunta_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
