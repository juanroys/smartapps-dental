{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Convenio</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nombreconvenio</th>
                <td>{{ entity.nombreConvenio }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('convenio') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('convenio_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
