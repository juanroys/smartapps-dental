{% extends '::base.html.twig' %}

{% block body -%}
    <h1>OpcionRespuesta</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Orden</th>
                <td>{{ entity.orden }}</td>
            </tr>
            <tr>
                <th>Valortexto</th>
                <td>{{ entity.valorTexto }}</td>
            </tr>
            <tr>
                <th>Valornumero</th>
                <td>{{ entity.valorNumero }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('opcionRespuesta') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('opcionRespuesta_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
