{% extends '::base.html.twig' %}

{% block body -%}
    <h1>OpcionRespuesta list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Orden</th>
                <th>Valortexto</th>
                <th>Valornumero</th>
                <th>Opciones</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('opcionrespuesta_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.orden }}</td>
                <td>{{ entity.valorTexto }}</td>
                <td>{{ entity.valorNumero }}</td>
                <td>{{ entity.opciones }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('opcionrespuesta_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('opcionrespuesta_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('opcionrespuesta_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
