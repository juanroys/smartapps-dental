{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Disponibilidad</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Diassemena</th>
                <td>{{ entity.diasSemena }}</td>
            </tr>
            <tr>
                <th>Fechadesde</th>
                <td>{{ entity.fechaDesde|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Fechahasta</th>
                <td>{{ entity.fechaHasta|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Horainicio</th>
                <td>{{ entity.horaInicio|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Horafin</th>
                <td>{{ entity.horaFin|date('Y-m-d H:i:s') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('disponibilidad') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('disponibilidad_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
